#include<stdio.h>
int a;
int b;
int c;

enum test{A,B,C};
enum {D=1,E,F};

int main()
{
	char *s = "ahdoahfo";
	a = b + c;
	
	return 0;
}

void translation_unit()
{

	char *d="abbfb";
	char ch = 'c';
	int b;
	b = a++;
	
	while ((*src >= 'a' && *src <= 'z') || (*src >= 'A' && *src <= 'Z') || (*src >= '0' && *src <= '9') || (*src == '_')) {
        hash = hash * 147 + *src;
		src++;
    }

	while(token != TK_EOF)
	{
		external_declaration(SC_GLOBAL);
	}
}